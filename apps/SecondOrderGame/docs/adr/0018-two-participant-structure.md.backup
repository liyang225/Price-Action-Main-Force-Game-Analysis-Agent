---
status: accepted
---

# W 矩阵压缩为二方参与者：主力与散户

## 背景

ADR-0007 设计 W 矩阵时预留了三个参与者维度：操纵型主力、配置型主力、羊群散户。这基于「操纵型与配置型在行为模式上有显著差异，且可从数据中观测」的假设。

经 ADR-0017 标注器实测与资金流向 API 考察后，发现单日数据层面**无法区分操纵型与配置型主力**：

1. **富途 capital flow API** 提供的分档（super/big/mid/small）只能区分大单（主力）与散单（散户），不携带资金性质（短线操纵还是长线配置）
2. **龙虎榜**（P4 计划接入）能区分游资席位与机构席位，但覆盖率仅 1-2%，且只在涨停/跌停/振幅异常日触发，无法支撑日常标注
3. **行为序列特征**（如连续五日小单吸筹 + 无拉升 + 板块景气度向好 = 配置型建仓）可以在观测层作为额外证据，但这需要跨日窗口分析，不是单日 OHLCV 可推断的

## 测量层的事实

在 t 日收盘时，标注器能直接输出的维度是：

- 板块情绪档位（冰点/启动/分歧/高潮/退潮）
- 个股行为（建仓/震仓/拉升/出货/观望/狩猎止损）
- （可选）主力 vs 散户的二分类（来自 capital flow 的大单占比）

标注器**无法**从 t 日数据直接输出「操纵型主力」还是「配置型主力」——这是序列层面的推断，不是单日观测。

## 决策

**将 W 矩阵参与者维度从 3 压缩为 2**：

```yaml
behavior_mapping:
  冰点:
    主力:      {建仓: 0.60, 震仓: 0.10, 拉升: 0.05, 出货: 0.05, 观望: 0.15, 狩猎止损: 0.05, alpha: 24}
    散户:      {建仓: 0.05, 震仓: 0.05, 拉升: 0.10, 出货: 0.30, 观望: 0.45, 狩猎止损: 0.05, alpha: 18}
```

从 5×3×6 = 90 个参数单元降至 5×2×6 = 60 个。

## 理由

### 1. 对齐测量层的真实粒度

W 矩阵的参与者维度必须与测量层能稳定输出的类别对齐。操纵型与配置型的区分是**推断层**的任务（P2 推演引擎根据序列特征 + 板块基本面 + 龙虎榜补充证据），不应下推到**标注层**——标注器输出的是确定性可验证的标签，推断层输出的是概率分布。

### 2. 样本量更充足

242 天滚动窗口 × 60 股 × 27% 覆盖率 = 3,931 样本点。

- 三方：3,931 ÷ 90 单元 = 43.7 样本/单元，但 狩猎止损（0.2% 命中率）在 15 个状态-参与者组合下各只有约 2 个样本
- 两方：3,931 ÷ 60 单元 = 65.5 样本/单元，狩猎止损的 10 个单元各约 3 个样本

仍然稀疏，但改善了 50%。

### 3. 架构文档已有支撑

`ARCHITECTURE.md` § 3.1 原文：「游资与同质化量化归入操纵型主力。价值型机构与长线外资归入配置型主力。本系统用户**不属于**羊群散户。」

用户反馈："我在项目架构里也说了，实际上就是散户羊群 VS 一个合并的主力，区分不太开也没有关系。"

系统的核心是二阶博弈：主力如何利用散户的情绪和从众行为、我方如何跳出散户视角。操纵型与配置型的差异是主力内部的子分类，不改变我方的应对逻辑。

### 4. 不影响推演层的丰富度

推演引擎（P2）仍然可以：

- 从序列特征（连续五日吸筹 + 无拉升）推断配置型主力概率
- 从龙虎榜（1-2% 覆盖）补充操纵型主力证据（游资席位频繁更换）
- 从板块基本面与政策环境调整「这是长线价值入场」还是「短线套利」的判断

只是这些推断**不体现在 W 矩阵的参与者索引里**，而是作为提示词的输入证据，或作为对 W 输出的后置贝叶斯调整。

## 后果

### 立即生效

1. **`config/hmm_prior.yaml`** 的 `behavior_mapping` 从三参与者改为两参与者，已有的操纵型/配置型行先验不丢失——两者的加权平均成为新的「主力」行
2. **`src/labeler_constants.py`** 的 `PARTICIPANTS = ["主力", "散户"]`
3. **所有测试** 从 `["操纵型主力", "配置型主力", "羊群散户"]` 改为 `["主力", "散户"]`
4. **`CONTEXT.md`** 与 **`ARCHITECTURE.md`** § 3.1 改写为两方参与者表述

### P0-4 板块层标注器解锁

板块层标注器的输出契约现在明确了：

```python
{
  "sector": "半导体",
  "date": "2026-08-10",
  "k120m_seq": 1,  # 午盘 or 收盘
  "sentiment_state": "分歧",
  "participants": {
    "主力": "吸筹",     # or 震仓/拉升/派发/观望（板块层用词与个股层略有差异，待 P0-4 规则确定）
    "散户": "恐慌抛售"
  }
}
```

不再需要区分操纵型与配置型。

### P4 龙虎榜接入时的处理

龙虎榜数据作为**观测证据注入推演提示词**，不改写 W 矩阵结构。例如：

```
提示词输入：
- 当日龙虎榜：3 个游资席位净买入 2.3 亿，机构席位 1 个净卖出 0.8 亿
- 板块情绪：启动期
- W 预测主力行为分布：{建仓: 0.45, 震仓: 0.20, ...}

推演引擎输出：
游资主导，短线套利概率高，建仓信号可信度下调 30%
```

而不是把 W 拆回三个参与者行。

## 与已有 ADR 的关系

- **ADR-0007**（事后标注器 Day 0 上线）：个股层标注器的输出从「(state, behavior)」扩展为「(state, participant, behavior)」——但 participant 只有两个值
- **ADR-0017**（个股层标注器规则冻结）：规则不变，只是输出 schema 增加 `"participant": "主力" | "散户"` 字段，由 capital flow 的大单占比决定
- **ADR-0001**（不训练只滤波）：W 的参数单元从 90 降至 60，先验强度不变，数据积累后的校准压力减轻

## 不采纳的替代方案

### 方案 A：保留三参与者，第三行标为永久先验

用户在上一个工单讨论中曾选择此方案："明确记为「永久先验，直至找到归属通道」"。

**不采纳理由**：这会让 W 矩阵包含两种性质的行——前两行有数据驱动的校准路径，第三行永远是专家猜测。审计时「先验权重」字段无法区分「数据还不够」和「这一行根本拿不到数据」，容易让用户误以为操纵型主力的概率也在收敛。

压缩为两方后，所有行的性质一致：Day 0 是先验，数据积累后均可校准。

### 方案 B：板块层标注器只输出 (state, behavior)，不输出 participant

保持原有 ADR-0007 设计，标注器不管参与者，由推演引擎自己猜。

**不采纳理由**：这会让 W 矩阵的参与者维度彻底失去数据锚点——W 索引的是 `[z][p][b]`，但喂进来的计数只有 `[z][b]`，无法知道哪个 p 做了这个 b。结果是 W 的三个参与者行永远无法分化，初始写的啥就是啥。

现在的设计让 capital flow 的大单/散单分档成为 participant 的观测依据，W 的两行可以朝不同方向校准。

---

**结论**：二方参与者结构对齐测量层真实粒度，解锁 P0-4 板块层标注器，为 P4 龙虎榜接入预留了推演层的接口，且不损失架构文档原有的「散户 VS 主力」二阶博弈核心框架。
