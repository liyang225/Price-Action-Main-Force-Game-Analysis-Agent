# 25 — 识别当前参与者行为

**What to build:** 读取冻结的分析材料，通过受约束模型调用识别主力或散户的当前行为候选，并保留支持证据。

**Blocked by:** 06 — 管理分析材料缓存生命周期; 23 — 启用生产提示词路由配置; 24 — 通过薄适配层复用 PA 模型客户端.

**Status:** ready-for-human

- [x] 输出参与者仅为主力或散户，行为仅为统一六类枚举。
- [x] 每个候选包含引用的输入证据，缺乏证据时返回明确的无法判断状态。
- [x] 非法 JSON、表外枚举和模型概率数字均不会被采信。

## Comments

- 2026-08-12：作为压缩交付包 A 完成。实现 `ParticipantClassifier`，复用受约束 PA 模型适配边界；无材料直接返回“无法判断”。
